name: CI - Build and Check Links
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true
defaults:
  run:
    shell: bash
jobs:
  build-and-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      HUGO_VERSION: 0.152.2
      NODE_VERSION: 22.20.0
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Create directory for user-specific executable files
        run: |
          mkdir -p "${HOME}/.local"
      - name: Cache Hugo binary
        id: hugo-cache
        uses: actions/cache@v4
        with:
          path: ${{ env.HOME }}/.local/hugo
          key: hugo-${{ env.HUGO_VERSION }}
      - name: Install Hugo
        if: steps.hugo-cache.outputs.cache-hit != 'true'
        run: |
          curl -sLJO "https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
          mkdir "${HOME}/.local/hugo"
          tar -C "${HOME}/.local/hugo" -xf "hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
          rm "hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
      - name: Add Hugo to PATH
        run: echo "${HOME}/.local/hugo" >> "${GITHUB_PATH}"
      - name: Verify installations
        run: |
          echo "Hugo: $(hugo version)"
          echo "Node.js: $(node --version)"
          echo "npm: $(npm --version)"
      - name: Install Node.js dependencies
        run: |
          npm ci
      - name: Build the site
        run: |
          npm run build
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: public-site
          path: ./public
          retention-days: 7
      - name: Setup lychee link checker
        uses: lycheeverse/lychee-action@v2
        with:
          args: >
            --verbose
            --no-progress
            --exclude 'https://linkedin.com'
            --root-dir '${{ github.workspace }}/public'
            --remap 'https://swiftenroll.github.io file://${{ github.workspace }}/public'
            './public/**/*.html'
          fail: true
